# VT Feeder Importer - Python Dependencies

# Required dependencies
google-cloud-storage>=2.10.0    # GCS SDK for fast downloads
mysql-connector-python>=8.0.0   # MySQL database driver

# Recommended dependencies (optional but strongly recommended)
orjson>=3.9.0                   # Fast JSON parser (2-3x faster than standard json)
